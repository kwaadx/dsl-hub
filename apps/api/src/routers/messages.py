"""Messages router placeholder.

This module is intentionally left minimal because message-related endpoints
are exposed via the threads router for the current MVP. Keeping this file
prevents import errors for future expansions and silences empty-module warnings.
"""
